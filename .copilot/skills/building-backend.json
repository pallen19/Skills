{
  "name": "building-backend",
  "summary": "Ensure backend tests run with proper Docker Compose build and containers readiness",
  "triggers": [
    "building backend",
    "run backend tests",
    "build backend"
  ],
  "description": "This skill knows that our backend microservices use Sticker and Sorbet. To run tests reliably we must run `docker compose up --build -d` and wait for all containers to be up and healthy before executing tests. When changes are made to `api` or `cir` services, containers must be rebuilt for changes to take effect.",
  "usage": [
    "When asked to run tests or verify the backend, run `docker compose up --build -d` and wait for containers to be healthy/running.",
    "Prefer `dotnet test <solution.sln>`; the skill will look for `--solution`, the `SOLUTION_NAME` env var, or the first `*.sln` file if not provided.",
    "If `dotnet` is not available, fall back to `make test` or `pytest`.",
    "A repo-provided `scripts/run_backend_tests.sh` is optional; if present the assistant may prefer to use it but must still ensure containers are rebuilt when `api/` or `cir/` change."
  ],
  "notes": "The skill prefers to run .NET tests via `dotnet test <solution>` (use `--solution` or `SOLUTION_NAME`), falls back to `make test` or `pytest` if needed. The skill will run `docker compose up --build -d` and wait for container readiness. If a repository includes `scripts/run_backend_tests.sh` the assistant may use it, but the skill must not depend on its presence.",
  "preferred_test_command": "dotnet test \"Solution Name\"",
  "script_optional": true,
  "run_steps": [
    "docker compose up --build -d",
    "wait for containers to report HEALTH=healthy or State=running",
    "dotnet test <solution> (discover with --solution, SOLUTION_NAME env, or first *.sln)",
    "fallback: make test or pytest"
  ],
  "examples": [
    {
      "prompt": "Run the backend test suite",
      "action": "Run `docker compose up --build -d` then wait for readiness and run `dotnet test <solution>` (or use `SOLUTION_NAME` or `--solution` to select)"
    },
    {
      "prompt": "I changed api; make sure tests run with rebuilt containers",
      "action": "Run `docker compose up --build -d` and re-run tests so containers pick up the changes"
    }
  ]
}